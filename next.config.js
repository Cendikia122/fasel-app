/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    output: 'export',
    distDir: 'dist',
  }
   
  module.exports = {
    output: 'standalone', // Untuk menjalankan Next.js dengan server
  };
  